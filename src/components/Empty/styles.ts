/** @format */

import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    borderColor: "#333333",
    borderTopWidth: 1,
    justifyContent: "center",
    alignItems: "center",
    gap: 16,
    height: 208,
  },

  text: {
    fontSize: 14,
    color: "#808080",
    fontWeight: "400",
  },
});
